import { InputStruct } from "./input-struct";

export class InputMapping {
    id: number;
    inpuStruct: InputStruct;
    identifier: string;
    columnNumber: number;
    startPosition: number;
    length: number
}